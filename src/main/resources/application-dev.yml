spring:
  datasource:
    url: jdbc:mysql://localhost:3307/sfg_dev?autoReconnect=true&useSSL=false
    username: sfg_dev_user
    password: devUser
    platform: mysql
    initialization-mode: always
  jpa:
      hibernate:
        ddl-auto: validate
      database-platform: org.hibernate.dialect.MySQL5Dialect
      database: mysql
      show-sql: true
#      properties:
#        javax:
#          persistence:
#            schema-generation:
#              create-source: metadata
#              scripts:
#                action: create
#                create-target: guru_database_create.sql
